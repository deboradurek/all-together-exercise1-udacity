import React from 'react';
import PropTypes from 'prop-types';

const User = (props) => (
  <li className="user">
    <p></p>
    <p>Number of games played: </p>
  </li>
);

User.propTypes = {};

export default User;
